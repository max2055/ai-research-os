# B-007 Discovery Service Handoff

Handoff ID：HOF-20260806-B007
日期：2026-08-06
发送人：Claude（Agent）
接收人：max（研究者）
Project ID：v0.3 Phase 2

## Scope

按 `08_Agent_Execution_Protocol` 工作包输入合同，本 WP = B-007 Discovery run
service（`03_Phase_2_Intelligence_Ingestion.md` B-007）：bounded orchestration
= lock、timeout、retry、audit。WP-201（B-005~007）就此全部完成。

## Rules and templates read

- `03_Phase_2_Intelligence_Ingestion.md`（§5.1 preflight、B-007）
- 现有 `source discover` CLI + `adapters/discovery.py`（WP-210 已实现）
- `08_Agent_Execution_Protocol.md`、`07_Templates/Research_Handoff.md`

## Objects created or changed

| Object | Action | Path | 说明 |
|---|---|---|---|
| discovery.py | 新增 | `src/research_os/services/discovery.py` | B-007 service |
| candidate_db.py | 扩展 | `src/research_os/services/candidate_db.py` | insert/record/list + 计数修复 |
| discover CLI | 代码 | `src/research_os/cli.py` + runtime | `discover run/due` |
| test_discovery.py | 新增 | `09_Automation/tests/test_discovery.py` | 5 测试 |
| Candidate DB 数据 | 数据 | `09_Automation/operational/candidates.db` | 真实试跑（gitignored）|

## Facts established

- **B-007 完整链路**：Channel 配置 → adapter 自动映射（rss/github/arxiv/sec）
  → preflight（reviewed/enabled/restricted/limit）→ discover → 写 Candidate DB
  （candidates + discovery_runs）。
- **`discover run --channel CHN-XXX [--apply]`**：dry-run 预览 / apply 写库。
  **`discover due`**：列出可调度 Channel。
- 真实试跑：SK hynix RSS 10 候选、arXiv 20 候选写入 Candidate DB。
- 修复 insert_candidates 计数 bug（`connection.total_changes` 是累计值，
  改为 `cursor.rowcount` 增量）。

## Inferences proposed

- 重复候选未去重（同一公告多 URL 变体如 `-01`/`-02`/`-` 全插入）——B-014
  dedup 是 Phase 2 下一个核心缺口（真实数据已暴露）。
- Candidate ID 用随机 `CND-<uuid>`；跨 run 同内容会产生重复行（预期，待 dedup）。

## Conflicts and unknowns

- 无冲突。validate 0 error；index 无 drift；137 tests；ruff/mypy 通过。
- Candidate DB 是 gitignored 派生数据，不在提交内。

## Human decisions required

- 无（纯工程交付；Channel 启用已由 max 授权）。

## Verification

- Validate：0 errors / 0 warnings
- Index：global + PRJ-001 + PRJ-002 全 PASS
- Tests：137 passed（+5 discovery）
- ruff / mypy：pass
- 真实：`discover due` = 4 可调度；SK hynix/arXiv run 成功写库

## Recommended next action

- **B-014（exact/near dedup）**：真实数据已暴露重复，dedup 是让 Candidate
  Queue 可用的前提。
- 或 B-017（scoring）→ B-018（Candidate Queue CLI）→ B-019（promote）。
