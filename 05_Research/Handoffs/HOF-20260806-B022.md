# B-022 Daily Brief Generator Handoff（WP-231 全部完成）

Handoff ID：HOF-20260806-B022
日期：2026-08-06
发送人：Claude（Agent）
接收人：max（研究者）
Project ID：v0.3 Phase 2

## Scope

按 `08_Agent_Execution_Protocol` 工作包输入合同，本交付 = B-022
（`03_Phase_2_Intelligence_Ingestion.md` B-022 / §8 Daily Brief）：pending
operational report，candidate/reviewed 分区。B-021（launchd runbook）+
本交付后，**WP-231（B-021～022）全部完成**，Master Backlog 置 completed。

## Rules and templates read

- `03_Phase_2_Intelligence_Ingestion.md`（B-022、§8 Daily Brief 八分区、§10 CLI）
- `08_Agent_Execution_Protocol.md`、`07_Templates/Research_Handoff.md`

## Objects created or changed

| Object | Action | Path | 说明 |
|---|---|---|---|
| brief.py | 新增 | `src/research_os/services/brief.py` | B-022 service |
| cli.py | 修改 | `src/research_os/cli.py` | `brief daily` |
| jobs.py / schemas/job.py | 修改 | `.../jobs.py`、`.../schemas/job.py` | `daily-brief` job |
| candidate_queue.py | 修改 | `src/research_os/services/candidate_queue.py` | singleton enrich fix |
| test_brief.py | 新增 | `09_Automation/tests/test_brief.py` | 4 测试 |
| test_schemas.py | 修改 | `09_Automation/tests/test_schemas.py` | job 计数基线 +2 |
| Daily Brief + Job 记录 | 新增 | `05_Research/Operations/Briefs/`、`Jobs/` | 真实产物 |
| Known_Limitations | 修改 | `00_System/Known_Limitations_v0.2.md` | B-022 |
| Master Backlog | 修改 | `09_Master_Backlog.md` | WP-231 → completed |

## Facts established

- **8 分区**（§8）：① 今日建议处理顺序 Top5 ② 新增高优先级（priority ≥ 0.5）
  ③ 已提升正式 Source（created_at=date）④ 新 reviewed Event（created_at=date
  且 review_status=reviewed）⑤ Core Company 影响（entity 属 core tier）⑥ 反面/
  冲突信号（ambiguous entity 或负面关键词）⑦ 论文/技术信号（arxiv channel）
  ⑧ 抓取失败（failed discovery_runs）+ stale Channel（到期未跑）+ coverage gap
  （从未跑）。
- **分区隔离**：候选分区每行标注 `unreviewed candidate（未审阅候选，非已审阅
  事实）`，与 reviewed Source/Event 分区物理隔离（§8 硬性要求）。
- **写路径**：`brief daily --date [--apply]` / `jobs run daily-brief --as-of`
  写 `05_Research/Operations/Briefs/Daily_Brief_<date>.md`，拒绝覆盖；job 幂等
  （已存在 → "already exists" success）。
- **附带修复 B-018 latent bug**：`enrich_candidates` 对 singleton 候选（无
  duplicate_cluster_id）误判 non-representative → 错加 dup_pen 0.5（相对排序
  掩盖，绝对分偏低）。现 singleton 视为自身代表。真实验证里旧分（0.620 等）
  不受影响（真实候选全有 cluster）。

## 真实验证

| CLI | 结果 |
|---|---|
| `brief daily --date 2026-08-06`（dry-run）| Top5 = CTI 0.620 / HBF 0.590 / SK+NVIDIA **ambiguous** 0.578 / …；Source/Event 分区 6/7 条 |
| `jobs run daily-brief --as-of 2026-08-06` | SUCCESS，brief 文件落盘 |
| 同 job 二次 | SUCCESS "already exists"（幂等）|
| validate / index | 0 errors / 全 PASS |

## Inferences proposed

- 反面信号 v1 是关键词/ambiguous 启发式；后续可接 LLM 或冲突检测（§C-006 层）。
- Daily Brief 与 launchd（B-021）可直接配 每日 1 次调度。

## Conflicts and unknowns

- 无冲突。validate 0 error；index 全 PASS；196 tests（+4）；ruff/mypy 通过。
- 已知局限：论文信号仅按 arxiv channel；反面信号无 NLP；Event 无 reviewed_at
  字段，按 created_at=date 近似"新 reviewed"。
- WP-231 现 completed（B-021 runbook / B-022 Daily Brief 全部交付）。

## Human decisions required

- 无（纯工程交付；brief 是 pending operational report，非权威事实）。
- 是否把 daily-brief 加入 launchd 调度由 max 决定（runbook 可加 StartCalendarInterval）。

## Verification

- Validate：0 errors / 0 warnings
- Index：global + PRJ-001 + PRJ-002 全 PASS
- Tests：196 passed（+4 brief）
- ruff / mypy：pass
- 真实：brief 分区 + 写文件 + job 幂等全验证

## Recommended next action

- **B-023～024（WP-232，metrics/health/secret redaction）**：Pipeline 可观测
  （§11 metrics：discovered rate/dup rate/fetch failure/promoted rate/dismissed
  rate）与 secret 不落盘（§13）。
- 或 max 决定把 discovery + daily-brief 纳入 launchd 并观察 1–2 周期。
