# B-023/024 Metrics + Secret Redaction Handoff（WP-232 全部完成）

Handoff ID：HOF-20260806-B023-024
日期：2026-08-06
发送人：Claude（Agent）
接收人：max（研究者）
Project ID：v0.3 Phase 2

## Scope

按 `08_Agent_Execution_Protocol` 工作包输入合同，本交付 = WP-232：
B-023（Metrics/observability：freshness、yield、noise、failure）+
B-024（secret redaction：token/cookie 不落盘）。完成后 **WP-232 全部完成**，
Master Backlog 置 completed。

## Rules and templates read

- `03_Phase_2_Intelligence_Ingestion.md`（B-023/B-024、§11 Metrics、§13 故障/
  secret redaction）
- `08_Agent_Execution_Protocol.md`、`07_Templates/Research_Handoff.md`

## Objects created or changed

| Object | Action | Path | 说明 |
|---|---|---|---|
| metrics.py | 修改 | `src/research_os/services/metrics.py` | pipeline_metrics + render |
| redaction.py | 新增 | `src/research_os/services/redaction.py` | B-024 |
| adapters/url.py | 修改 | `src/research_os/adapters/url.py` | canonicalize_url 丢敏感参数 |
| discovery.py | 修改 | `src/research_os/services/discovery.py` | 候选 URL 落库前 redact |
| jobs.py | 修改 | `src/research_os/services/jobs.py` | job message 写盘前 redact |
| ui/app.py | 修改 | `src/research_os/ui/app.py` | /operations Pipeline health 面板 |
| cli.py / runtime | 修改 | `src/research_os/cli.py`、`runtime/product.py` | `pipeline metrics` |
| test_pipeline_metrics.py / test_redaction.py | 新增 | `09_Automation/tests/` | +8 tests |
| Known_Limitations | 修改 | `00_System/Known_Limitations_v0.2.md` | B-023/024 |
| Master Backlog | 修改 | `09_Master_Backlog.md` | WP-232 → completed |

## Facts established

- **pipeline_metrics（§11）**：discovered（total/today）、duplicate rate
  （非代表候选/cluster）、discovery failure rate + 失败列表、median latency、
  core entity 覆盖、promoted/dismissed rate + top dismiss reasons、median
  candidate→promote hours、stale/never-run channels（复用 B-021 due_channels）、
  http/parse errors/retries/cost。
- **dashboard**：`ui/app.py` `/operations` 加 Pipeline health 面板（discovered、
  dup/failure rate、promoted/dismissed、stale/never-run、median latency）。
- **secret redaction（B-024）**：`redact_secrets` 把 token/api_key/key/
  password/secret/signature/auth/session/cookie 查询参数值与
  Authorization/Bearer/Cookie 头值替换为 `<REDACTED>`；`canonicalize_url`
  **丢弃**敏感查询参数（URL 不落盘）；discovery 候选 canonical_url 落库前
  redact（且 fingerprint 与存储 URL 一致）；jobs 记录 message 写盘前 redact。
- 真实指标：dup rate 75%（15 非代表/5 cluster，SK hynix 批次高度重复）、
  1 core 实体、CHN-skhynix-ir 标 stale（每 6h 应再跑未跑）。

## 真实验证

| CLI | 结果 |
|---|---|
| `pipeline metrics --as-of 2026-08-06` | 20 候选 today、dup 75%、0 失败、1 core、stale 1/never-run 3 |
| redaction 单测 | canonicalize_url 丢 token；redact_secrets 覆盖 query/header；候选记录 redacted；job message redacted |
| validate / index / 204 tests | 0 errors / 全 PASS / 204 passed（+8）|

## Inferences proposed

- dup rate 75% 说明 SK hynix RSS 多 URL 变体噪声高——promote 前以 cluster
  代表为主。
- cost/token 未跟踪是 §11 缺口，留 B-025 Pilot 时补。

## Conflicts and unknowns

- 无冲突。validate 0 error；index 全 PASS；204 tests（+8）；ruff/mypy 通过。
- 已知局限：model token 未跟踪（report None）；median latency 对旧 run 为 0
  （旧代码 finished_at=started_at）；cost_estimate 大多未填；secret redaction
  覆盖已知模式（非正则万能）。

## Human decisions required

- 无（纯工程交付；pipeline metrics 是只读观测，redaction 是防御性边界）。

## Verification

- Validate：0 errors / 0 warnings
- Index：global + PRJ-001 + PRJ-002 全 PASS
- Tests：204 passed（+8 pipeline_metrics/redaction）
- ruff / mypy：pass
- 真实：pipeline metrics + redaction 边界全验证

## Recommended next action

- **B-025～026（WP-240，14-day real Pilot + Phase acceptance）**：连续运行
  与人工 triage（§12 Gate：≥20 reviewed/enabled Channel、Top-20 人工相关性
  ≥75%、≥20 promote、资产/hash 检查）。
- 或 max 决定把 launchd（B-021）正式装上，跑 1–2 周期再进 Pilot。
