# v0.3 Release Gate Verification

Status: passed
Date: 2026-08-12
Timezone: Asia/Shanghai
Commit: 4f188e855ad4a1a3de4bab27d413da6669ce0e34

## Scope and authority boundary

This record verifies the current F-023 quality and Dashboard security/smoke
evidence against the exact commit above. It does not approve v0.3, close F-024,
create a release or tag, alter a Thesis, or make any Impact Assertion authoritative.
All 22 Impact Assertions remain `review_status: pending`.

## Quality verification

| Gate | Scope | Result |
|---|---|---|
| full pytest suite | non-local repository suite | pass; 734 passed, 1 skipped, 38 local-integration tests deselected |
| Coverage | `research_os`, branch coverage | pass; 80.30% against the unchanged >=80% Gate |
| Ruff lint and format | complete tracked Python tree | pass; lint clean and 1,690 files formatted |
| mypy and compileall | 121 source files and bytecode compilation | pass |

The non-local pytest run emitted two `ResourceWarning` entries for unclosed SQLite
connections in the existing discovery parse-failure test. They did not fail the
suite or reduce coverage below the Gate and are recorded rather than suppressed.
The 38 local-integration tests were run separately without coverage instrumentation;
their pre-evidence result was 36 passed and two expected F-023 failures because this
verification record did not yet exist. Those two checks are rerun after this record
is present and are not counted as a passing result in the table above.

## Dashboard smoke and security

Dashboard GET smoke returned HTTP 200 for `/`, `/home`, `/impact`, `/analysis`,
`/decision`, `/operations`, `/health`, `/companies/COM-nvidia`,
`/sectors/SEG-memory-storage`, and `/pipeline/queue`.
No Web mutation was used.

| Gate | Scope | Result |
|---|---|---|
| Dashboard route smoke | ten real loopback GET routes on the repository | pass; 10/10 HTTP 200 |
| Dashboard security suite | external text escaping, traversal rejection, secret redaction, bounded query inputs, mutation allowlist | pass; 5/5 tests |
| Dashboard mutation allowlist | non-GET application routes | pass; only `/llm/config`, `/llm/models`, and `/llm/test` |

## Repository integrity

| Gate | Result |
|---|---|
| doctor | pass; Python 3.13.9, 1,141 objects, 0 warnings, 0 drift, 0 asset failures |
| strict repository validation | pass; 0 errors and 0 warnings |
| global, PRJ-001, and PRJ-002 indexes | pass; all match canonical output |
| operational Candidate DB | read-only recovery capture; integrity `ok`, schema user version 2 |
| research authority | pass; 22/22 Impact Assertions pending, no Thesis change |
| release invariant | pass; no `v0.3*` tag created and no release/push performed |

## Evidence provenance

The quality tree is the committed `src/research_os`, `09_Automation/tests`,
`requirements`, and `pyproject.toml` state at the recorded commit. The Dashboard
security tree is `src/research_os/ui/app.py` plus
`09_Automation/tests/test_m6_security.py` at the same commit. F-023 rejects this
record if those paths diverge from the recorded commit or contain uncommitted
changes.
