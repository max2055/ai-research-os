# v0.3 Discovery Job Audit: 2026-08-10

Status: passed
Review status: reviewed
Audit date: 2026-08-12
Reviewer: max
Audit decision: approve
Scope: retained discovery and retention Job Runs created on 2026-08-09, plus
the two applied arXiv recovery runs created on 2026-08-11

This audit describes operational evidence. It does not approve research objects,
change a Thesis, or establish an investment conclusion. Human review is required
before treating the operational judgment as authoritative.

## Scope and method

The fixed audit manifest is the 30 Job Run files from
`JOB-20260809154712-001` through `JOB-20260809220127-001` enumerated in
`09_Automation/tests/test_operational_job_audit.py`. The files are retained at
`05_Research/Operations/Jobs/` byte-for-byte from the scheduler output.

At evidence capture, the audit queried `09_Automation/operational/candidates.db`
through a SQLite `mode=ro` connection. The queried database SHA-256 was
`f12e1a510fd553e10e9f5dddbf88f32693ace92c3e508bdec8230b92ff921689`.
The 28 matched `discovery_runs` rows were reduced to run metadata in
`09_Automation/tests/fixtures/operational_job_audit_runs.json`; no Candidate row,
title, URL, content, or secret is present in that fixture.

Default regression tests read the committed metadata fixture and the committed Job
files only. They do not locate or open an ignored operational database, so they run
in a clean clone or Git archive. The fixture preserves the captured reconciliation
boundary; it is not independent evidence and does not represent live database state.

An explicit live recheck is available when the operator supplies the database path:

```bash
RESEARCH_OS_AUDIT_CANDIDATE_DB=/absolute/path/candidates.db \
  PYTHONPATH=src /Users/max/.venvs/ai-research-os/bin/python -m pytest \
  09_Automation/tests/test_operational_job_audit.py -m local_integration -q
```

The live test opens only that environment-supplied path with SQLite `mode=ro`,
compares the selected rows with the fixture and Job mapping, and verifies the file
hash is unchanged during the query. Without the environment variable it skips; it
does not infer another checkout path.

Local sources used:

- Title: 30 retained AI Research OS Job Runs; publisher: AI Research OS scheduler;
  recorded: 2026-08-09; accessed: 2026-08-10; local path:
  `05_Research/Operations/Jobs/JOB-20260809*-001-*.md`, limited to the fixed test
  manifest.
- Title: AI Research OS Candidate operational database; publisher: AI Research OS
  discovery service; run dates: 2026-08-09; accessed: 2026-08-10; local path:
  `09_Automation/operational/candidates.db`; table: `discovery_runs`; SHA-256:
  `f12e1a510fd553e10e9f5dddbf88f32693ace92c3e508bdec8230b92ff921689`.

For the retained-incident evidence capture above, no external endpoint was queried
and no transport retry was performed. The separate read-only arXiv check and the
two later applied recovery runs are recorded under Post-fix verification below.

## Facts

- The fixed manifest contains exactly 30 Job Runs: 28 `discover` and two `expire`.
- The Job outcomes are 28 `success` and two `failed`. Both failures are discovery
  Jobs; both expire Jobs succeeded.
- The 26 successful discovery Jobs each contain one unique
  `RUN-[0-9a-f]{16}` identifier. For every one, the message channel equals the Job
  target and the corresponding read-only `discovery_runs` row has the same run ID,
  channel and candidate count. The Candidate run status is `succeeded`, while the
  Job status vocabulary is `success`.
- Both expire messages are exactly `retention sweep: 0 expired, 0 purged`.
- Every retained Job has `started_at` less than or equal to `finished_at`.
- The two failed Job messages retain
  `SSL: UNEXPECTED_EOF_WHILE_READING`; neither record was converted to success or
  omitted from the audit.

These facts are assertions over the local operational records listed above and are
enforced by `09_Automation/tests/test_operational_job_audit.py`.

## Retained incidents

| Job | Channel | Candidate run | Status | Retained observation |
|---|---|---|---|---|
| JOB-20260809154712-001 | CHN-arxiv | RUN-5103a52bd05d4b1c | failed | Job message contains `SSL: UNEXPECTED_EOF_WHILE_READING`. |
| JOB-20260809154743-001 | CHN-arxiv-agents | RUN-7aa75664f4d74282 | failed | Job message contains `SSL: UNEXPECTED_EOF_WHILE_READING`. |

The original Job messages remain in the Job Run files. This table shortens them for
readability and does not replace the primary records.

## Candidate run reconciliation

The read-only `discovery_runs` rows provide the following failed-run match:

| Job | Job interval (UTC) | Candidate run interval (UTC) | Channel match | Run status |
|---|---|---|---|---|
| JOB-20260809154712-001 | 15:47:12.309566-15:47:36.471828 | RUN-5103a52bd05d4b1c: 15:47:31-15:47:36 | CHN-arxiv | failed |
| JOB-20260809154743-001 | 15:47:43.861532-15:48:07.949918 | RUN-7aa75664f4d74282: 15:48:02-15:48:07 | CHN-arxiv-agents | failed |

In both cases the Candidate run has the same channel as the Job, has status
`failed`, and starts and finishes within the enclosing Job interval. The executable
test also checks the 26 successful discovery rows individually rather than relying
on aggregate counts.

## Inference

The recorded SSL exception is consistent with a transport interruption during the
two arXiv requests. That is an inference from the error string, not an independently
proven root cause. The retained records do not distinguish among a remote endpoint
termination, an intervening network or proxy condition, a local TLS-stack condition,
or another transient transport failure. The successful non-failing discovery Jobs
show that other scheduled work completed in the audited windows, but they do not
prove that either failed arXiv path was healthy.

## Operational judgment

Retain both failed Job Runs and their failed Candidate runs as immutable incident
evidence. Do not relabel them based on later success elsewhere. A transport change
and a new, separately identified run should be reviewed before considering the two
channels operationally recovered. This judgment changes no Source, Evidence,
Thesis, confidence, Forecast, Recommendation, or investment conclusion.

## Post-fix verification

Historical status before the implementation commit:
Status: **pending until transport fix**.

Current status: **both arXiv channels have separately identified, applied recovery
runs and the operational recovery audit is approved**.

During the UTC window `2026-08-10T05:23:10Z` through
`2026-08-10T05:23:30Z`, the feature implementation at commit `d4c7467` ran this
command against the real main repository without `--apply`:

```bash
PYTHONPATH=/Users/max/Coding/57-AI-Research-OS/.worktrees/v03-operational-hardening/src \
  /Users/max/.venvs/ai-research-os/bin/python -m research_os \
  --root /Users/max/Coding/57-AI-Research-OS \
  discover run --channel CHN-arxiv
```

The command exited `0` and reported dry-run ID `RUN-7bad4f31707f4cf5`, 20
Candidates, one fetch attempt, zero retries, zero HTTP errors, and zero parse
errors. These Candidates were output proposals only; none was inserted.

Before and after the command, the real Candidate database SHA-256 was
`10090bacf1aa0a7d9ac2b1f6b91be21c77c50311d2f5e419c3aeaf505bb96fdc` and the
count of `05_Research/Operations/Jobs/*.md` was `201`. Hash equality and Job-count
equality therefore show that this check did not write the Candidate database or a
Job record.

This is a fact about one read-only `CHN-arxiv` request at the recorded time. It did
not establish the root cause of the 2026-08-09 incidents, exercise
`CHN-arxiv-agents`, create an applied Candidate run, or establish scheduler
recovery by itself.

On 2026-08-11 UTC, the scheduler then completed two new applied discovery Jobs:

| Job | Channel | Candidate run | Job status | Run status | Candidate count | Errors / retries |
|---|---|---|---|---|---:|---|
| JOB-20260811233232-001 | CHN-arxiv | RUN-f8bba6ff28794282 | success | succeeded | 20 | 0 / 0 / 0 |
| JOB-20260811233308-001 | CHN-arxiv-agents | RUN-f684480c013a44de | success | succeeded | 20 | 0 / 0 / 0 |

The fixed Job files have SHA-256 values
`af3e2c3ee09b42cc63aad3c392777208631301de04b7cfbdd615b670a30c118e` and
`dfb80fd2094fc44f4e953c80e4aa5510dac43a89fd95d46202c55c5980a9ddbc`.
A read-only SQLite capture found both `discovery_runs` rows with the channel and
candidate count shown above, `integrity_check = ok`, schema `user_version = 2`, and
database SHA-256
`d7e3347cc85f7e5bafd8d3c361c1133121b22ef2ed7a63acf250bf93890b2fa2`.
The sanitized run metadata is retained in
`09_Automation/tests/fixtures/operational_job_recovery_runs.json`.

The `candidates` table has no `discovery_run_id`, so this audit does not claim a
row-by-row Candidate-to-run association. It establishes Job-to-run reconciliation
and run-level counts only. The original two failed Jobs and Candidate runs remain
unchanged as incident evidence.

## Human operational decision

On 2026-08-12, max approved the operational judgment that the two channels have
completed separately identified applied recovery runs and that there is no
unresolved silent miss or unauthorized capture in this audited scope. This approval
applies only to operational evidence. It does not approve any Candidate, Source,
Event, Impact Assertion, Thesis, confidence change, Forecast, Recommendation, or
investment conclusion.
