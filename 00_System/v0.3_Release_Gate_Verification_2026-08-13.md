# v0.3 Release Gate Verification

Status: passed
Date: 2026-08-13
Timezone: Asia/Shanghai
Commit: d8a07e4b0347f3a15f1a681d5952a35102cc6fc0

## Scope and authority boundary

This record verifies the current F-023 quality and Dashboard security/smoke evidence
against the exact commit above after F-027A Candidate Web parity. It does not approve
v0.3, close F-024, create a release or tag, alter a Thesis, or make a pending research
object authoritative. The five time-dependent or named-human F-023 blockers remain.

## Quality verification

| Gate | Scope | Result |
|---|---|---|
| full pytest suite | non-local repository suite | pass; completed to 100% with zero failures and 1 skip |
| Coverage | `research_os`, branch coverage | pass; 80.85% against the unchanged >=80% Gate |
| Ruff lint and format | complete tracked Python tree | pass; lint clean and 184 Python files formatted |
| mypy and compileall | 126 source files and bytecode compilation | pass |

The coverage run emitted two `ResourceWarning` entries for unclosed disposable SQLite
connections in the existing discovery parse-failure test. They did not fail the suite,
reduce coverage below the Gate, or indicate an integrity failure in the real Candidate
store. The warnings remain visible rather than being suppressed.

## Dashboard smoke and security

Dashboard GET smoke returned HTTP 200 for `/`, `/home`, `/impact`, `/analysis`,
`/decision`, `/operations`, `/health`, `/companies/COM-nvidia`,
`/sectors/SEG-memory-storage`, and `/pipeline/queue`.
No Web mutation was used.

| Gate | Scope | Result |
|---|---|---|
| Dashboard route smoke | ten current-repository HTTP application routes | pass; 10/10 HTTP 200 and zero mutation requests |
| Dashboard security suite | escaping, traversal, secrets, bounded forms, Host/Origin/session/CSRF, signed preview, replay/stale target, compensation and atomic rollback | pass; focused Web/security/capability suite clean |
| Dashboard mutation allowlist | exact non-GET application routes | pass; three `/llm/*` plus Candidate dismiss/restore/promote preview and commit, nine total |
| Candidate mutation smoke | disposable repository through real loopback HTTP | pass; hostile Origin 403; restore/promote preview 200, commit 303, result 200, replay 409 |

The disposable smoke produced exactly one restore and one promote action, one committed
audit for each operation, one pending Source record, and two immutable Source assets.
Promotion preview did not render captured body text. Thesis and Review tree digests were
unchanged. The smoke Source ID existed only in the disposable repository.

## Repository integrity

| Gate | Result |
|---|---|
| strict repository validation | pass; 1164 formal objects, 0 errors, 0 warnings |
| global index | pass; canonical output matches, zero drift |
| operational Candidate DB | schema user version 3; mutation transaction and compensation tests pass |
| research authority | pass; no Thesis or Review mutation in Candidate smoke |
| release invariant | pass; no v0.3 tag, release, push, or approval created |

## Evidence provenance

The quality tree is the committed `src/research_os`, `09_Automation/tests`,
`requirements`, and `pyproject.toml` state at the recorded commit. The Dashboard
security tree is `src/research_os/services/candidate_db.py`, `mutation_audit.py`,
`mutation_gateway.py`, `triage.py`, `web_identity.py`, `src/research_os/ui/app.py`,
`09_Automation/tests/test_m6_security.py`, and
`09_Automation/tests/test_web_mutation.py` at the same commit. F-023 rejects this record
if any covered path diverges from the recorded commit or contains uncommitted changes.
