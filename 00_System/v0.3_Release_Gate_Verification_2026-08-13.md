# v0.3 Release Gate Verification

Status: passed
Date: 2026-08-13
Timezone: Asia/Shanghai
Commit: 6337ccda9b2087b5a58c25cffc14304002f47647

## Scope and authority boundary

This record refreshes F-023 quality provenance after the F-026 governance and
documentation close-out. It does not approve v0.3, close F-024, create a release or
tag, alter a Thesis, or make any Impact Assertion authoritative. All 22 Impact
Assertions remain `review_status: pending`.

## Quality verification

| Gate | Scope | Result |
|---|---|---|
| full pytest suite | non-local repository suite | pass; 755 passed, 1 skipped, 38 local-integration tests deselected |
| Coverage | `research_os`, branch coverage | pass; 80.66% against the unchanged >=80% Gate |
| Ruff lint and format | complete tracked Python tree | pass; lint clean and 180 tracked Python files formatted |
| mypy and compileall | 124 source files and bytecode compilation | pass |

The coverage run emitted two `ResourceWarning` entries for unclosed SQLite
connections in the existing discovery parse-failure test. They did not fail the
suite or reduce coverage below the Gate and remain recorded rather than suppressed.

## Dashboard smoke and security

The security and Web mutation suite was rerun on 2026-08-13. The real TCP loopback
GET and disposable mutation smokes were run on 2026-08-12; every covered security
implementation and test path is byte-identical between that smoke commit and the
current commit recorded above.

Dashboard GET smoke returned HTTP 200 for `/`, `/home`, `/impact`, `/analysis`,
`/decision`, `/operations`, `/health`, `/companies/COM-nvidia`,
`/sectors/SEG-memory-storage`, and `/pipeline/queue`.
No Web mutation was used.

| Gate | Scope | Result |
|---|---|---|
| Dashboard route smoke | ten real loopback GET routes on the repository | pass; 10/10 HTTP 200 on 2026-08-12 |
| Dashboard security suite | external text escaping, traversal rejection, secret redaction, bounded inputs, Host/Origin/session/CSRF, signed preview, nonce/replay/stale-target, and atomic rollback | pass; 22/22 focused security and Web mutation tests on 2026-08-13 |
| Dashboard mutation allowlist | non-GET application routes | pass; three `/llm/*` routes plus Candidate dismiss preview and commit only |
| Candidate mutation smoke | disposable Candidate store through HTTP application boundary | pass; detail 200, hostile Origin 403, preview 200, commit 303, result 200, replay 409 on 2026-08-12 |

The disposable mutation smoke issued an `HttpOnly; SameSite=Strict` browser session,
left preview read-only, then produced exactly one Candidate action and one committed
mutation audit in the same successful transaction. It did not approve a Source,
Evidence, Review, Impact Assertion, Thesis, confidence change, or release decision.

## Repository integrity

| Gate | Result |
|---|---|
| doctor | pass; Python 3.13.9, 1,155 objects, 0 warnings, 0 drift, 0 asset failures |
| strict repository validation | pass; 1,155 objects, 0 errors and 0 warnings |
| global, PRJ-001, and PRJ-002 indexes | pass; all match canonical output |
| operational Candidate DB | current integrity `ok`, schema user version 3 |
| research authority | pass; 22/22 Impact Assertions pending, no Thesis change |
| release invariant | pass; no `v0.3*` tag created and no release/push performed |

## Evidence provenance

The quality tree is the committed `src/research_os`, `09_Automation/tests`,
`requirements`, and `pyproject.toml` state at the recorded commit. The Dashboard
security tree is `src/research_os/services/candidate_db.py`,
`mutation_audit.py`, `mutation_gateway.py`, `triage.py`, `web_identity.py`,
`src/research_os/ui/app.py`, `09_Automation/tests/test_m6_security.py`, and
`09_Automation/tests/test_web_mutation.py` at the same commit. F-023 rejects this
record if any covered path diverges from the recorded commit or contains uncommitted
changes.
