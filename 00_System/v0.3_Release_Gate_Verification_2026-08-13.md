# v0.3 Release Gate Verification

Status: passed
Date: 2026-08-13
Timezone: Asia/Shanghai
Commit: b0e8d1aa7fe491f29357da8f767f9427ac48190f

## Scope and authority boundary

This record verifies the current F-023 quality and Dashboard security/smoke evidence
against the exact commit above after F-027B Source/Evidence/Review Web parity. It does not approve
v0.3, close F-024, create a release or tag, alter a Thesis, or make a pending research
object authoritative. The five time-dependent or named-human F-023 blockers remain.

## Quality verification

| Gate | Scope | Result |
|---|---|---|
| full pytest suite | non-local repository suite | pass; 802 passed, 1 skipped, 38 local-integration tests deselected |
| Coverage | `research_os`, branch coverage | pass; 80.69% against the unchanged >=80% Gate |
| Ruff lint and format | complete tracked Python tree | pass; lint clean and 192 Python files formatted |
| mypy and compileall | 130 source files and bytecode compilation | pass |

The coverage run emitted two `ResourceWarning` entries for unclosed disposable SQLite
connections in the existing discovery parse-failure test. They did not fail the suite,
reduce coverage below the Gate, or indicate an integrity failure in the real Candidate
store. The warnings remain visible rather than being suppressed.

## Dashboard smoke and security

Dashboard GET smoke returned HTTP 200 for `/`, `/home`, `/impact`, `/analysis`,
`/decision`, `/operations`, `/health`, `/companies/COM-nvidia`,
`/sectors/SEG-memory-storage`, and `/pipeline/queue`.
No Web mutation was used.

| Gate | Scope | Result |
|---|---|---|
| Dashboard route smoke | ten real loopback HTTP routes on the recorded commit | pass; 10/10 HTTP 200 and zero mutation requests |
| Dashboard security suite | escaping, traversal, secrets, bounded forms, Host/Origin/session/CSRF, signed preview, replay/stale target, fixed human authority, compensation and atomic rollback | pass; complete suite and focused Web/security/capability suite clean |
| Dashboard mutation allowlist | exact non-GET application routes | pass; 23 exact routes: three `/llm/*`, six Candidate, eight Source, two Event, two Report and two Review routes |
| Source/Evidence/Review mutation smoke | disposable repository through real loopback HTTP | pass; hostile Origin 403; preview 200, commit 303, result 200, replay 409; Source/Event/Report pending by default and Event reviewed only by explicit human Review |

The disposable F-027A smoke produced one restore and one promote action. The F-027B
smoke created and processed a Source, created and explicitly reviewed an Event, and
created a pending Report. Audit rows recorded preview/commit/replay outcomes, captured
body text was absent from confirmation HTML, and unrelated Thesis and pre-existing
Review tree digests were unchanged. All smoke artifacts existed only in disposable
repositories.

## Repository integrity

| Gate | Result |
|---|---|
| strict repository validation | pass; 1185 formal objects, 0 errors, 0 warnings |
| global, PRJ-001 and PRJ-002 indexes | pass; canonical output matches, zero drift |
| operational Candidate DB | schema user version 3; mutation transaction and compensation tests pass |
| research authority | pass; no Thesis or Review mutation in Candidate smoke |
| release invariant | pass; no v0.3 tag, release, push, or approval created |

## Evidence provenance

The quality tree is the committed `src/research_os`, `09_Automation/tests`,
`requirements`, and `pyproject.toml` state at the recorded commit. The Dashboard
security tree covers the repository transaction, Candidate DB, mutation audit/gateway,
capability registry, Candidate/Source/Event/Report/Review Web adapters, fixed Web
identity, UI application, and every corresponding security, capability, parity and
atomicity test in `09_Automation/tests`. The exact path set is machine-checked by
`test_dashboard_security_provenance_covers_web_mutation_boundary`. F-023 rejects this
record if any covered path diverges from the recorded commit or contains uncommitted
changes.
