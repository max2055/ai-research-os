# v0.3 Release Gate Verification

Status: passed
Date: 2026-08-18
Timezone: Asia/Shanghai
Commit: 002cac5e68b2844dd4a4e5763b2a0599976cc968

## Scope and authority boundary

This record verifies the current engineering and Web-hosted scheduler integration
candidate after the cutover from launchd and Markdown operational scheduling. It does
not approve v0.3, close F-024, create a release or tag, alter a Thesis, or make a
pending research object authoritative. The five time-dependent or named-human F-023
blockers remain.

## Quality verification

| Gate | Scope | Result |
|---|---|---|
| full pytest suite | complete repository suite | pass; 960 passed, 2 skipped |
| Coverage | `research_os`, branch coverage | pass; 83.56% against the unchanged >=80% Gate |
| Ruff lint and format | complete tracked Python tree | pass; lint clean and format clean |
| mypy and compileall | complete source tree and bytecode compilation | pass |

The coverage run emitted existing ResourceWarning entries for disposable SQLite
connections in legacy tests. They did not fail the suite, reduce coverage below the
Gate, or indicate an integrity failure in the Candidate or operations store.

## Dashboard smoke and Web-hosted worker

Dashboard GET smoke returned HTTP 200 for `/`, `/home`, `/impact`, `/analysis`,
`/decision`, `/operations`, `/health`, `/companies/COM-nvidia`,
`/sectors/SEG-memory-storage`, and `/pipeline/queue`.
No Web mutation was used.

| Gate | Scope | Result |
|---|---|---|
| Dashboard route smoke | ten real loopback HTTP routes | pass; 10/10 HTTP 200 |
| Operations route smoke | `/operations/schedules`, `/operations/jobs`, `/operations/backups`, `/health` on the same loopback server | pass; 4/4 HTTP 200 |
| Worker lifecycle smoke | website start, fresh `worker_state` heartbeat, graceful website stop | pass; Worker PID exited and state recorded `stopped` |
| Dashboard security suite | escaping, traversal, secrets, Host/Origin/session/CSRF, signed preview, replay/stale target, fixed human authority and atomic rollback | pass; focused Web/security/capability suites clean |
| Dashboard mutation allowlist | exact non-GET application routes | pass; capability registry covers research, registry, operations and release preview/commit routes; no product CLI route is required |

The disposable Web mutation tests cover schedule create/edit/pause/resume/run-now,
manual Job enqueue, one-use confirmation, optimistic version checks, audit rows and
the restriction that automation cannot approve research objects or change Thesis
conclusion/confidence. New runs are authoritative in `operations.db`; no new
`JOB-*.md` record is written.

## Backup and recovery

| Gate | Result |
|---|---|
| operations control plane | pass; WAL mode, foreign keys, lease recovery, heartbeat, audit and atomic enqueue/finalize tests |
| durable backup | pass; candidate archive includes verified `operations.db` snapshot, manifest SHA-256 and schema version |
| disposable restore | pass; candidate, operations database and Source assets restore with integrity checks |
| strict repository validation | pass; no new validation error introduced by the scheduler cutover |
| research authority | pass; no Thesis or Review mutation in scheduler or backup tests |
| release invariant | pass; no v0.3 tag, release, push or approval created |

## Repository integrity and provenance

The quality tree is the committed `src/research_os`, `09_Automation/tests`,
`requirements`, and `pyproject.toml` state at the recorded commit. The Dashboard
security tree covers the repository transaction, Candidate DB, operations database,
mutation audit/gateway, capability registry, schedule mutations, Worker supervisor,
UI application, and corresponding security, capability, parity and atomicity tests.
The exact path set is machine-checked by
`test_dashboard_security_provenance_covers_web_mutation_boundary`. F-023 rejects this
record if any covered path diverges from the recorded commit or contains uncommitted
changes.

F-023 remains **16/21** with exactly five independent blockers: real WP-620 30-day
Pilot completion, WP-530 natural Forecast resolutions (earliest 2026-10-31), two
Weekly plus one Monthly cadence reviews, named human Known Limitations
acknowledgement, and named F-024 release approval. Engineering evidence does not
close or weaken them.

The 2026-08-13 verification record remains historical and unchanged. This record is
the current engineering evidence for the Web-hosted Worker scheduler cutover.
