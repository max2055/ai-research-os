# v0.3 Performance Benchmark

Status: passed
Date: 2026-08-10
Timezone: Asia/Shanghai
Commit: `814e13f`
Scope: F-012 profile/caching decision and F-013 real/scale SLO evidence

## Decision

The current single-user read models pass the Phase 6 SLOs after eliminating repeated YAML loads inside one request. The implementation uses one validated in-memory object list per snapshot call and no persistent cache. Markdown remains authoritative; every new request loads current repository state.

## Real repository scale

- Formal objects: 1034
- Candidate rows: 1490
- Companies: 59
- Sectors: 9
- Impact Assertions: 22
- Ontology Assertions: 260
- Benchmark repeats: 3
- Method: nearest-rank p95
- Read-only check: authoritative writes: 0

Command:

```bash
PYTHONPATH=src /Users/max/.venvs/ai-research-os/bin/python -c \
  'from pathlib import Path; from research_os.services.benchmark import benchmark_dashboard; print(benchmark_dashboard(Path("."), repeats=3).as_dict())'
```

| Operation | Samples (s) | p95 (s) | SLO (s) | Result |
|---|---|---:|---:|---|
| Home | 1.005872, 0.961944, 0.960026 | 1.005872 | <2 | pass |
| Candidate Queue | 0.954656, 0.962148, 0.953859 | 0.962148 | <2 | pass |
| Company | 0.949863, 0.948755, 0.962799 | 0.962799 | <2 | pass |
| Sector | 0.952237, 0.950448, 0.965617 | 0.965617 | <2 | pass |
| Impact 3-hop | 0.954301, 0.958419, 0.961600 | 0.961600 | <3 | pass |
| Full validate | 0.946722, 0.975344, 0.985800 | 0.985800 | <30 | pass |
| Index render | 0.002074, 0.001875, 0.001836 | 0.002074 | <30 | pass |

## Profile and correction

Before correction, the same benchmark measured Home p95 7.675100s and Company p95 2.906065s. cProfile showed:

- Home called `validate_repository` 8 times and loaded 8272 Markdown documents.
- Company called `validate_repository` 3 times and loaded 3102 Markdown documents.
- YAML parsing dominated cumulative time; Candidate SQL and path expansion were not the bottleneck.

The correction added pure `*_from_objects` query helpers. `industry_home_snapshot` and `company_snapshot` now validate once and pass the same immutable object list to Daily Brief, Channel due, Forecast status, Action, metrics, and coverage calculations. This is request-scoped preloading, not stale authority and not a cross-request cache.

## Synthetic scale

Command:

```bash
PYTHONPATH=src /Users/max/.venvs/ai-research-os/bin/python -m research_os \
  --root . benchmark --sources 1000 --events 500 --max-seconds 30
```

| Measure | Result |
|---|---:|
| Sources | 1000 |
| Events | 500 |
| Total objects | 1501 |
| Validation errors | 0 |
| Validation | 1.4105s |
| Global index | 0.0029s |
| Project index | 0.0028s |
| Total | 1.4161s |
| Maximum | 30.0s |
| Decision | pass |

## Limits

This is a local single-user measurement on the current machine, not a multi-user or 100k-Candidate result. PostgreSQL, a graph database, and a persistent cache remain untriggered. Candidate Queue currently has 1490 rows, so the Phase 6 10k filter SLO is not claimed as measured; it remains a scale limitation until a 10k operational fixture is run.
