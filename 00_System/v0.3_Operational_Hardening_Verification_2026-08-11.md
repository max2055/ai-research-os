# v0.3 Operational Hardening Verification

Status: merged local verification passed; final main CI pending
Date: 2026-08-11
Timezone: Asia/Shanghai
Feature branch: `codex/v03-operational-hardening`
Feature branch head: `7129dca`
Matching feature-branch CI: run `31450602779`, conclusion `success`
Preservation commit on main: `1d0a517`
Merge commit on main: `b02027c`
Merged local gate completed: 2026-08-11

## Scope and authority boundary

This record verifies the deliverable MVP operational-hardening scope. It does not
approve a v0.3 release, create a release decision, or replace human research review.
All 22 Impact Assertions remain `review_status: pending`; the agent audit contains
recommendations only and creates no reviewer, decision, approval, or `reviewed_at`.

The natural-time and human release gates remain independent blockers:

| Gate | State | Required real evidence |
|---|---|---|
| `WP-530` | `BLOCKED` | Forecast outcomes after their natural resolution dates; earliest 2026-10-31 |
| `WP-620` | `BLOCKED` | Real 30-day Pilot and natural resolutions |
| `F-024` | `BLOCKED` | Human release packet and decision after the preceding gates |

No synthetic outcome, historical evidence, human identity, approval, v0.3 tag, or
v0.3 product release is created by this work.

## Delivered MVP scope

- Discovery transport uses bounded retries and records terminal run metrics.
- Candidate Queue has deterministic pagination and a disposable 10k-row benchmark.
- Explicit v0.3 release evaluation is read-only and independently reports the three
  blockers above while preserving the v0.2 contract.
- Private hosted CI uses metadata-only validation and does not receive raw Source
  assets, the Candidate DB, backup material, provider credentials, or identities.
- Durable backup encrypts Candidate and Source-asset sets, binds all GitHub CLI calls
  to the canonical host, verifies remote hashes, and restores only to a disposable
  destination.
- Health distinguishes local and durable backup state, reports a newer durable
  failure, and retains P1 stale/missing/invalid behavior.
- Monthly provider cost monitoring remains secret-safe and does not treat missing or
  invalid data as zero.
- The pending Impact Assertion audit narrows only repository-verifiable metadata and
  confidence bounds; it does not widen evidence parsing or make authoritative review
  decisions.
- The repository Ruff formatting baseline is isolated in a mechanical commit.

## Discovery and performance evidence

The retained 2026-08-09 incident batch remains immutable: 30 Jobs, 28 successes and
2 failures. Both failed arXiv Jobs retain their original
`SSL: UNEXPECTED_EOF_WHILE_READING` messages. A separate read-only post-fix request
at commit `d4c7467` exited 0 for `CHN-arxiv`, returned 20 Candidate proposals in one
attempt with zero retries/HTTP/parse errors, and left both the Candidate DB hash and
201-Job count unchanged. This proves one read-only request succeeded; it does not
rewrite the incidents or claim applied two-channel scheduler recovery.

The disposable Candidate Queue benchmark at commit `6bf5ca7` used 10,000 rows, one
warmup, seven measured repetitions, page size 200, and offset 200. Samples were
`1.017968`, `1.013581`, `1.007352`, `1.023611`, `1.015772`, `1.034069`, and
`1.017175` seconds; nearest-rank p95 was `1.034069` seconds against the `<2.0`
second SLO. The real Candidate store hash and all 1,074 tracked formal-object paths
were unchanged, so the conditional migration/index gate did not trigger.

## Pending Impact Assertion audit

| Result | Evidence |
|---|---|
| Scope | 22 assertions, all assigned to `PRJ-001` |
| Authority | 22/22 remain `review_status: pending` |
| Agent recommendations | 16 ready for human review, 2 edit required, 4 reject recommended |
| Safeguards | no reviewer, decision, approval, `reviewed_at`, Review Decision, or Thesis confidence increase |
| Project indexes | `PRJ-001` contains 22; `PRJ-002` contains 0 |

The structured audit is
`05_Research/Reviews/Impact_Assertion_Agent_Audit.json`; the deterministic human-review
packet is `05_Research/Reviews/Impact_Assertion_Agent_Audit_Packet.md`.

## Durable backup verification

The ignored local configuration was updated to canonical repository
`github.com/max2055/ai-research-os`. The repository was verified private and the fixed
`research-os-durable-backups-v1` release was verified as a prerelease. Recipient and
private identity values were not printed or persisted in tracked files.

| Check | Result |
|---|---|
| Dry-run preflight | pass; no receipt, remote asset, or Job created |
| Applied Job | `JOB-20260811012433-001`, status `success` |
| Backup | `BKP-20260811T012435Z-7936055bf65a`, status `verified`, 2 sets |
| Candidate ciphertext | 2,274,024 bytes; remote size/hash verified |
| Source-asset ciphertext | 160,510,408 bytes; remote size/hash verified |
| Explicit remote verification | pass; both sets downloaded and hash-checked without decryption |
| Disposable restore | pass; Candidate schema 2 and 400 Source asset files |
| Candidate integrity | SQLite `integrity_check` returned `ok`; schema user version 2 |
| Logical comparison | live and restored `.dump` SHA-256 both `fbcd1592639bd909d7daa9470edd1e901c63cf143e68d090d4b377d3736a08a7` |
| Logical counts | both stores: 2,167 candidates, 207 discovery runs, 4 tables |

The restore stayed in a disposable `/tmp` directory and did not overwrite the live
Candidate DB or authoritative Source asset tree. The successful Job and ignored
receipt remain operational evidence to preserve through the main-worktree merge.

## Final unified verification

Private hosted CI run `31450602779` passed against the exact feature-branch head
`7129dcad9e2c98d24901e0c91f1d78fde6c83737`. The branch was then merged locally
without rewriting the main worktree's operational evidence. Before the merge, 85
previously untracked Job records were preserved in commit `1d0a517`; the 30 records
that overlapped the feature branch were byte-identical, with zero mismatches. The
canonical global, `PRJ-001`, and `PRJ-002` indexes were regenerated from the merged
247-Job corpus in merge commit `b02027c`.

The complete local gate then ran against the merged main tree. The ignored runtime
asset and Candidate DB hashes remained identical to the pre-preservation baseline.
Only this verification record changed after that complete gate; the focused final
pre-push checks are rerun before pushing the resulting main SHA.

| Gate | Result |
|---|---|
| doctor | pass; Python 3.13.9, 1,065 objects, 0 warnings, 0 drift, 0 asset failures |
| strict repository validation | pass; 0 errors and 0 warnings |
| global, `PRJ-001`, and `PRJ-002` index checks | pass; all match canonical output |
| full pytest suite | pass; 100% complete, with 2 documented skips |
| Ruff lint and format | pass; lint clean and 1,669 files formatted |
| mypy and compileall | pass; mypy clean across 121 source files |
| ten real loopback Dashboard routes | pass; all returned HTTP 200 |
| Dashboard write-route allowlist | pass; only `/llm/config`, `/llm/models`, and `/llm/test` |
| v0.2 release check | pass; Ready, 18/18 gates |
| v0.3 release check | expected `BLOCKED`; 21 checks with independent `WP-530`, `WP-620`, and `F-024` failures |
| tracked runtime/secret audit | pass; no raw asset, Candidate DB, ciphertext, identity, private key, ignored file, or detected live secret tracked |
| research authority audit | pass; no Thesis change, no new Review Decision, 22/22 Impact Assertions pending |
| Job preservation | pass; 247 Job objects retained, including the original 2 failed incident records |
| ignored runtime integrity | pass; pre-preservation and post-merge hashes are identical |
| release invariant | pass; no `v0.3*` Git tag or GitHub product release exists |

## Delivery decision

The operational-hardening feature branch passed matching private CI and was merged
losslessly into local main. The final main SHA still requires push-event CI before
delivery is complete. This is an MVP delivery record, not a v0.3 release decision.
v0.3 release status remains `BLOCKED` until `WP-530`, `WP-620`, and `F-024` are
satisfied with real evidence.
