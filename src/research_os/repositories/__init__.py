"""Persistence adapters for Markdown and derived views.

Import concrete repositories from their modules so the legacy compatibility
entry point can run before optional product dependencies are installed.
"""
